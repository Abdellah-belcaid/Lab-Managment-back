# MySQL database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/Lab-Managment
spring.datasource.username=belcaid
spring.datasource.password=belcaid2001

# MySQL driver class and dialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JPA properties
  # Display SQL queries in console
spring.jpa.show-sql=true             
  # Update the database schema automatically
spring.jpa.hibernate.ddl-auto=update
